version: "3.9"

services:
  cart:
    build: 
      context: .
      dockerfile: HelloMicroservices/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "8087:80"
    links:
      - cartdb
  cartdb:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "cartapp"
      POSTGRES_PASSWORD: "cartpw"
    volumes:
      - ./HelloMicroservices/database-scripts/:/docker-entrypoint-initdb.d
